{
    "name": "the-last-of-guss",
    "private": true,
    "version": "1.0.0",
    "packageManager": "pnpm@10.8.0",
    "scripts": {
        "dev": "turbo run dev --parallel",
        "dev:f": "turbo run dev --filter=frontend",
        "dev:b": "turbo run dev --filter=backend",
        "build": "turbo run build",
        "build:f": "turbo run build --filter=frontend",
        "build:b": "turbo run build --filter=backend",
        "mg": "turbo run mg --filter=backend",
        "lint": "turbo run lint",
        "typecheck": "turbo run typecheck",
        "test": "turbo run test"
    },
    "dependencies": {
        "@types/node": "^24.0.14",
        "eslint": "^9.30.1",
        "typescript": "^5.8.3"
    },
    "devDependencies": {
        "turbo": "^2.5.5"
    }
}